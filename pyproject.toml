[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "festo-cpx-io"
description = "Library to control and access festo CPX modules"
readme = {file = "README.md", content-type = "text/markdown"}
authors = [
    {name = "Martin Wiesner", email = "martin.wiesner@festo.com"},
    {name = "Elias Rosch", email = "elias.rosch@festo.com"},
]
license = {text = "MIT"}
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dynamic = ["version"]
requires-python = ">=3.10"
dependencies = [
    'pymodbus>=3.0.0,<4.0.0', 
    'rich'
]

[project.urls]
Homepage = "https://gitlab.festo.company/festo/electric-automation/remote-io/festo-cpx-io"
Documentation = "https://gitlab.festo.company/festo/electric-automation/remote-io/festo-cpx-io"
Repository = "https://gitlab.festo.company/festo/electric-automation/remote-io/festo-cpx-io"
Changelog = "https://gitlab.festo.company/festo/electric-automation/remote-io/festo-cpx-io"

[project.scripts]
festo-cpx-io = "cpx_io.cli.cli:main"

[tool.setuptools.package-data]
parameter_map = ["cpx_io/cpx_system/data/parameter_map.csv"]

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["tests"]

[tool.setuptools_scm]